apiVersion: 1

providers:
  - name: "observatory"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards   # mounted from ./dashboards/ — the DASHBOARDS port
      foldersFromFilesStructure: true
